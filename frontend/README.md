# Frontend - Projeto Full-Stack

Frontend em Vue 3 (Vite) que consome a API do backend.

Configuração:

1. Instale dependências

```bash
cd frontend
npm install
cp .env.example .env
```

2. Rodar dev server

```bash
npm run dev
```

Endereços usados:

- VITE_API_URL - url base da API/backend

